[project]
name = "unit-converter"
version = "1.0.0"
description = "A command-line unit converter for length, weight, and temperature"
requires-python = ">=3.8"

[project.scripts]
unit-converter = "src.main:main"
